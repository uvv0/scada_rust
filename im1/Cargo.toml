[package]
name = "im1"
version = "0.1.0"
edition = "2021"

[dependencies]
byteorder = "1.5.0"
eframe = "0.31.1"
chrono = "0.4"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["rt-multi-thread", "net", "time", "sync"] }
toml = "0.8"
