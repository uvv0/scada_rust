Работаем в проекте:
- C:\andr\my2\ss5

Состояние на сейчас (12 Feb 2026):

1) KPZ I/O Script: POST начал корректно видеть размер приема
- Было: `POST 20: 0`, `POST 22: 0`.
- Сделано:
  - после PRE обновляется база из `70010/400`;
  - добавлен fallback `rv(20)` / `rv(21)` помимо `rv(base+20/21)`.
- Результат: `POST 20=148`, `POST 21=74`, `POST 22=2` (как ожидалось).

2) KPZ I/O Script: обработка `emit(...)` из POST
- Было: применялись только `reg(...)`, `emit(...)` игнорировался.
- Сделано:
  - добавлена обработка `post_out.emits` с записью в `reg.val`;
  - маппинг ключа: сначала пробуем как `reg.id`, затем fallback как `mb`.

3) Важный фикс БД по `reg.val`
- Причина, почему `5021` не обновлялся:
  - поле `reg.val` в БД имеет тип `text`, а не numeric.
- Сделано:
  - `update_reg_val` и `update_reg_val_checked` пишут строку (`val.to_string()`).
- Результат:
  - после скрипта `emit 5021=...` значение внизу появляется.

4) Отображение Input (TIT)
- Сделан fallback отображения:
  - сначала `last_val` (живой опрос),
  - если его нет, берется `reg_val` из БД.
- Это убирает `val=-` когда live-опрос не вернул значение.

5) TU режим в KPZ I/O
- Для групп с `n_mb=1` (команда 5) скрывается блок `Holding (n_mb = REG)` и `Write holding`, чтобы не путало оператора.

6) Диагностический лог скрипта
- В `Script log` сейчас есть временные строки:
  - `[POST] result regs=... emits=...`
  - `[POST] emit ...`
- Можно убрать после финальной проверки (не критично, но желательно).

Собранный exe:
- C:\andr\my2\ss5\target\release\ss5.exe

Проверка в новой сессии:
1. Выбрать KPZ, опросить группу 18 (чтобы заполнить сервисные/индексные данные).
2. Переключиться на группу 17, нажать `Script`.
3. Проверить в `Script log`:
   - `POST 20=148`, `POST 21=74`, `POST 22=2`
   - наличие `emit 5021=...`
4. Проверить в нижнем списке Input:
   - регистр `5021` отображает значение (не `-`).

DB (для справки):
- jdbc:postgresql://localhost:5432/postgres_restored
- user: uvv0
- pass: z
